diff --git a/javacall/implementation/psp_mips/jsr135_mmapi/multimedia.c b/javacall/implementation/psp_mips/jsr135_mmapi/multimedia.c
index d50b3d7..e2c4f1a 100644
--- a/javacall/implementation/psp_mips/jsr135_mmapi/multimedia.c
+++ b/javacall/implementation/psp_mips/jsr135_mmapi/multimedia.c
@@ -249,19 +249,6 @@ void *__wrap_open_file(char *name, int decompress, int noise_mode)
     extern void *ctl;
     extern char sdl_control_mode[];
 
-    {
-        extern void javacall_diag_log(const char *s) __attribute__((weak));
-        static int logged;
-        unsigned int gpv;
-        asm volatile("move %0,$gp" : "=r" (gpv));
-        if (logged < 4 && javacall_diag_log != 0) {
-            char line[128];
-            logged++;
-            sprintf(line, "media: open_file gp=%08x want=%08x ctl=%p\n",
-                    gpv, (unsigned int) (size_t) _gp, ctl);
-            javacall_diag_log(line);
-        }
-    }
 
     if (ctl == NULL) {
         ctl = (void *) sdl_control_mode;
@@ -366,18 +353,6 @@ javacall_result javacall_media_initialize(void) {
         return JAVACALL_FAIL;
     }
 
-    {
-        extern void javacall_diag_log(const char *s) __attribute__((weak));
-        int freq = 0, channels = 0;
-        Uint16 format = 0;
-        Mix_QuerySpec(&freq, &format, &channels);
-        if (javacall_diag_log != 0) {
-            char line[96];
-            sprintf(line, "media: spec freq=%d format=0x%x channels=%d\n",
-                    freq, (unsigned int) format, channels);
-            javacall_diag_log(line);
-        }
-    }
 
     music_handle_occupied = 0;
     /* Start both mixers at full scale. A MIDlet that never touches
@@ -494,17 +469,6 @@ javacall_handle javacall_media_create(javacall_int64 playerId,
     /* TEMPORARY -- the gameplay-silence hunt: every create attempt with
        its mime, straight to the diagnostic log (javacall_printf's console
        channels do not reach the memory stick). */
-    {
-        extern void javacall_diag_log(const char *s) __attribute__((weak));
-        static int logged;
-        if (logged < 40 && javacall_diag_log != 0) {
-            char line[112];
-            logged++;
-            sprintf(line, "media: create mime=%.48s len=%ld\n",
-                    (pszMime != NULL) ? pszMime : "(null)", contentLength);
-            javacall_diag_log(line);
-        }
-    }
 
     /* MIDI, including the Scalable Polyphony profile. SP-MIDI is ordinary
        MIDI with a scalability header that Timidity ignores, and it is common
@@ -2255,18 +2219,6 @@ static long buffer_wav(wav_player_handle* mp, const void* buffer, long length, l
     		}
     	}
     	
-   	{
-       	extern void javacall_diag_log(const char *s) __attribute__((weak));
-       	static int logged;
-       	if (logged < 24 && javacall_diag_log != 0) {
-       	    char line[128];
-       	    logged++;
-       	    sprintf(line, "media: wav endbuf len=%ld chunk=%p err=%.48s\n",
-       	            mp->buffered_len, (void *) chunk,
-       	            (chunk == NULL) ? Mix_GetError() : "-");
-       	    javacall_diag_log(line);
-       	}
-    	}
    	if(!chunk) {
        	javacall_printf("Mix_LoadWAV(): %s\n", Mix_GetError());
        	// this might be a critical error...
@@ -2341,17 +2293,6 @@ static javacall_result start_wav(wav_player_handle* mp) {
         chunk = mp->chunk;
     }
     
-    {
-        extern void javacall_diag_log(const char *s) __attribute__((weak));
-        static int logged;
-        if (logged < 24 && javacall_diag_log != 0) {
-            char line[96];
-            logged++;
-            sprintf(line, "media: start_wav chunk=%p ch=%d\n",
-                    (void *) chunk, mp->channel);
-            javacall_diag_log(line);
-        }
-    }
     if (chunk) {
     	javacall_printf("Mix_PlayChannel %d...\n", mp->channel);
     	if ((mp->channel = Mix_PlayChannel(mp->channel, chunk, 0)) == -1) {
