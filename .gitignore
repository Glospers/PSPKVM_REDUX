# --- Upstream source, not vendored -------------------------------------------
# Fetched fresh at the pinned commits (see docs/INVESTIGATION.md and the CI
# workflow). Kept out of the repo so history stays small and the GPLv2/EPL
# source boundary stays clean.
/pspkvm/
/m3gcore/
/pspgl/

/.debug/

# --- Build artifacts ---------------------------------------------------------
*.o
*.a
*.so
*.elf
EBOOT.PBP
PARAM.SFO
**/output/
/m3g/obj/
/m3g/lib/
/m3g/test/obj/
/m3g/test/m3g_load_test
/m3g/test/m3g_render_test
/build/
/dist/
/ppsspp_win/
/working pspkvm/
/ke_emulator/

# --- OS / editor -------------------------------------------------------------
.DS_Store
Thumbs.db
*.swp

# Local disposable build tree (mounted into the container)
/build-src/

# Dead-end diagnostic patches kept locally for reference only
